.One-message
  .One-message__prof
    .One-message__prof--name
      = message.user.name
    .One-message__prof--time
      = message.created_at.strftime("%Y年%m月%d日 %H時%M分")
  .One-message__contents
    - if message.content.present?
      %p.Message__content
        = message.content
    = image_tag message.image.url, class: 'Message__image' if message.image.present?